// src/types.ts

export interface TeamMember {
  name: string;
  position: string;
  img: string;
}

export interface Icon {
  iconPath: string;
  color: string;
}

export interface Metric {
  number: string;
  label: string;
}
